{% set name = "pyfive" %}
{% set version = "1.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyfive-{{ version }}.tar.gz
  sha256: b34616b7827d42c5f1b81689e1a25b3f6d05e9364de2eb89447c22b04419017f

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip !=21.3
    - setuptools >=80
    - setuptools_scm >=8
  run:
    - python >={{ python_min }}
    - numpy >=2
    - typing_extensions

test:
  source_files:
    - tests
  requires:
    - python {{ python_min }}
    - pip
    - botocore <1.43.53
    - dask
    - flask
    - flask-cors
    - h5netcdf
    - h5py
    - moto
    - netcdf4 !=1.7.4  # github.com/Unidata/netcdf4-python/issues/1464
    - pytest
    - pytest-cov
    - pytest-html
    - pytest-xdist
    - python-neo-lzf
    - s3fs >=2025.9.0
  commands:
    - pip check
    - pytest --ignore=tests/test_filter_pipeline_v2.py::test_hdf5_filters
    - p5dump -h
  imports:
    - pyfive

about:
  home: https://github.com/NCAS-CMS/pyfive
  license: BSD-3-Clause
  license_file: LICENSE.txt
  summary: 'A pure Python HDF5 file reader'

extra:
  recipe-maintainers:
    - davidhassell
    - valeriupredoi
    - jjhelmus
